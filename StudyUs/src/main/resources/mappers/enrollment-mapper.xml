<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="enrollmentMapper">

	<resultMap type="Enrollment" id="enrollmentResultMap">
		<id property="enrollmentNo" column="EN_NO"></id>
		<result property="memberNo" column="MB_NO"></result>
		<result property="studyNo" column="ST_NO"></result>
		<result property="insertDate" column="EN_INSERTDATE"></result>
		<result property="message" column="EN_MESSAGE"></result>
		<result property="status" column="EN_STATUS"></result>
	</resultMap>
	
	<resultMap type="com.studyus.study.domain.StudyWithEnrollmentList" id="studyWithEnrollmentListResultMap">
		<association property="study" javaType="Study">
		
		</association>
		<collection property="hashtagList" ofType="com.studyus.hashtag.domain.Hashtag" autoMapping="true">
			<id property="hashtagNo" column="HA_NO" javaType="int"></id>
			<result property="name" column="HA_NAME" javaType="String"></result>
		</collection>
	</resultMap>

	<insert id="insertEnrollment" parameterType="Enrollment">
		INSERT INTO ENROLLMENT VALUES (SEQ_ENROLLMENT.NEXTVAL,
										#{memberNo},
										#{studyNo},
										SYSDATE,
										#{message},
										DEFAULT)
	</insert>
	
	<select id="selectAllByStudyNo" resultMap="enrollmentResultMap">
		
	</select>
	
	<!--
	<select id="checkUrl" resultType="int">
		SELECT COUNT(*) FROM STUDY WHERE ST_URL = #{url}
	</select>
	
	<select id="selectOneByUrl" resultType="Study">
		SELECT * FROM STUDY WHERE ST_URL = #{url}
	</select>
	
	<select id="selectAllEnrolledByMemberNo">
		SELECT S.ST_NO, S.ST_NAME, S.ST_URL FROM STUDY S JOIN ENROLLMENT E ON S.ST_NO = E.ST_NO WHERE EN_STATUS = 1 AND E.MB_NO = ${memberNo};
	</select> -->
	
	<!-- 
	<select id="selectNoticeList" resultMap="noticeResultMap">
		SELECT * FROM NOTICE ORDER BY N_CREATE_DATE DESC
	</select>
	
	<select id="selectOne" resultMap="noticeResultMap">
		SELECT * FROM NOTICE WHERE NOTICE_NO = #{noticeNo }
	</select>
	
	<select id="selectSearchList" resultMap="noticeResultMap">
		SELECT * FROM NOTICE
		<if test="searchCondition.toString() == 'writer'">
			WHERE NOTICE_WRITER LIKE '%' || #{searchValue } || '%'
		</if>
		<if test="searchCondition.toString() == 'title'">
			WHERE NOTICE_TITLE LIKE '%' || #{searchValue } || '%'
		</if>
		<if test="searchCondition.toString() == 'content'">
			WHERE NOTICE_CONTENTS LIKE '%' || #{searchValue } || '%'
		</if>
		<if test="searchCondition.toString() == 'all'">
			WHERE NOTICE_WRITER LIKE '%' || #{searchValue } || '%'
			OR NOTICE_TITLE LIKE '%' || #{searchValue } || '%'
			OR NOTICE_CONTENTS LIKE '%' || #{searchValue } || '%'
		</if>
		ORDER BY N_CREATE_DATE DESC
	</select>

	<insert id="insertNotice" parameterType="Notice">
		INSERT INTO NOTICE VALUES(SEQ_NOTICEID.NEXTVAL, #{noticeTitle }, #{noticeContents }, #{noticeWriter }, DEFAULT, DEFAULT, #{noticeFilepath })
	</insert>
	
	<update id="updateNotice" parameterType="Notice">
		UPDATE NOTICE 
		SET NOTICE_TITLE = #{noticeTitle }, NOTICE_CONTENTS = #{noticeContents }, NOTICE_FILEPATH = #{noticeFilepath }, N_UPDATE_DATE = SYSDATE
		WHERE NOTICE_NO = #{noticeNo }
	</update>
	
	<delete id="deleteNotice" parameterType="_int">
		DELETE FROM NOTICE WHERE NOTICE_NO = #{noticeNo }
	</delete>
	 -->
</mapper>
